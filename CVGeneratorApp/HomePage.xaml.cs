using System;
using System.Windows;
using System.Windows.Controls;

namespace CVGeneratorApp
{
    public partial class HomePage : Page
    {
        public HomePage()
        {
            InitializeComponent();

            // Note: In the future, we will write a function here to load 
            // real data from the SQL database into the 'pnlCVList' WrapPanel.
        }
    }
}