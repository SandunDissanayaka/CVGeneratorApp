<UserControl x:Class="CVGeneratorApp.EducationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White" FontFamily="Segoe UI">

    <UserControl.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        <Style x:Key="ModernInput" TargetType="Control">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="10,5"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Education Level" Style="{StaticResource FieldLabel}"/>
            <ComboBox x:Name="cmbEduLevel" Style="{StaticResource ModernInput}" SelectionChanged="cmbEduLevel_SelectionChanged">
                <ComboBoxItem Content="University Degree / Diploma" IsSelected="True"/>
                <ComboBoxItem Content="G.C.E. Advanced Level (A/L)"/>
                <ComboBoxItem Content="G.C.E. Ordinary Level (O/L)"/>
            </ComboBox>

            <TextBlock Text="Institution / School Name" Style="{StaticResource FieldLabel}"/>
            <ComboBox x:Name="cmbInstitution" IsEditable="True" Style="{StaticResource ModernInput}">
                <ComboBoxItem Content="University of Sri Jayewardenepura"/>
                <ComboBoxItem Content="University of Moratuwa"/>
                <ComboBoxItem Content="University of Colombo"/>
                <ComboBoxItem Content="University of Kelaniya"/>
                <ComboBoxItem Content="University of Peradeniya"/>
                <ComboBoxItem Content="University of Ruhuna"/>
                <ComboBoxItem Content="Sri Lanka Institute of Information Technology (SLIIT)"/>
                <ComboBoxItem Content="Informatics Institute of Technology (IIT)"/>
                <ComboBoxItem Content="NSBM Green University"/>
                <ComboBoxItem Content="General Sir John Kotelawala Defence University (KDU)"/>
                <ComboBoxItem Content="National Institute of Business Management (NIBM)"/>
                <ComboBoxItem Content="APIIT Sri Lanka"/>
            </ComboBox>

            <StackPanel x:Name="pnlDegree" Visibility="Visible">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Start Year" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="cmbDegreeStart" IsEditable="True" Style="{StaticResource ModernInput}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="End Year" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="cmbDegreeEnd" IsEditable="True" Style="{StaticResource ModernInput}"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Degree / Qualification" Style="{StaticResource FieldLabel}"/>
                <ComboBox x:Name="cmbDegree" IsEditable="True" Style="{StaticResource ModernInput}">
                    <ComboBoxItem Content="BSc (Hons) in Information Technology"/>
                    <ComboBoxItem Content="BSc (Hons) in Computer Science"/>
                    <ComboBoxItem Content="BSc (Hons) in Software Engineering"/>
                    <ComboBoxItem Content="BSc (Hons) in Information Systems"/>
                    <ComboBoxItem Content="BSc (Hons) in Data Science"/>
                    <ComboBoxItem Content="BSc (Hons) in Cyber Security"/>
                    <ComboBoxItem Content="Bachelor of Information Technology (BIT)"/>
                    <ComboBoxItem Content="BSc in Business Management"/>
                </ComboBox>

                <TextBlock Text="GPA / Class" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="txtGPA" Style="{StaticResource ModernInput}"/>
            </StackPanel>

            <StackPanel x:Name="pnlAL" Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Examination Year" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="cmbALYear" IsEditable="True" Style="{StaticResource ModernInput}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="Index Number" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="txtALIndex" Style="{StaticResource ModernInput}"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Stream" Style="{StaticResource FieldLabel}"/>
                <ComboBox x:Name="cmbStream" IsEditable="True" Style="{StaticResource ModernInput}">
                    <ComboBoxItem Content="Physical Science (Maths)"/>
                    <ComboBoxItem Content="Biological Science"/>
                    <ComboBoxItem Content="Commerce"/>
                    <ComboBoxItem Content="Arts"/>
                    <ComboBoxItem Content="Technology"/>
                </ComboBox>

                <TextBlock Text="Results Summary (e.g., 2A, 1B / Z-Score: 1.85)" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="txtALResults" Style="{StaticResource ModernInput}"/>
            </StackPanel>

            <StackPanel x:Name="pnlOL" Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Examination Year" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="cmbOLYear" IsEditable="True" Style="{StaticResource ModernInput}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="Index Number" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="txtOLIndex" Style="{StaticResource ModernInput}"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Results Summary (e.g., Passed with 9 A's)" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="txtOLResults" Style="{StaticResource ModernInput}"/>
            </StackPanel>

        </StackPanel>

        <Button x:Name="btnAddEducation" Grid.Row="1" Content="+ Add Education" 
                HorizontalAlignment="Left" Padding="20,10" Margin="0,5,0,20"
                Background="Transparent" BorderBrush="#007ACC" Foreground="#007ACC" 
                FontWeight="Bold" Cursor="Hand" Click="btnAddEducation_Click">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Button.Resources>
        </Button>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="lstEducation">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F9F9F9" CornerRadius="8" Padding="15" Margin="0,0,0,10" BorderBrush="#EEEEEE" BorderThickness="1">
                            <Grid>
                                <StackPanel>
                                    <TextBlock Text="{Binding MainTitle}" FontWeight="Bold" FontSize="15" Foreground="#333333"/>
                                    <TextBlock Text="{Binding Institution}" FontSize="13" Foreground="#666666" Margin="0,2,0,5"/>
                                    <TextBlock Text="{Binding Details}" FontSize="12" Foreground="#888888" TextWrapping="Wrap"/>
                                </StackPanel>
                                <Button Content="Remove" HorizontalAlignment="Right" VerticalAlignment="Center" 
                                        Background="Transparent" Foreground="#FF4444" BorderThickness="0" Cursor="Hand"
                                        Click="RemoveEducation_Click"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>