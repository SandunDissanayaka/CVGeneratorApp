<UserControl x:Class="CVGeneratorApp.PersonalInfoControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White" FontFamily="Segoe UI">

    <UserControl.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="White" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#007ACC"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,40,0">

            <TextBlock Text="Full Name" Style="{StaticResource FieldLabel}"/>
            <TextBox x:Name="txtFullName" Style="{StaticResource ModernTextBox}" Tag="e.g. Sandun Dissanayaka"/>

            <TextBlock Text="Professional Title" Style="{StaticResource FieldLabel}"/>
            <ComboBox x:Name="cmbTitle" IsEditable="True" Height="38" Margin="0,0,0,20" Padding="10,5">
                <ComboBoxItem Content="Software Engineer"/>
                <ComboBoxItem Content="Full Stack Developer"/>
                <ComboBoxItem Content="IT Undergraduate"/>
                <ComboBoxItem Content="UI/UX Designer"/>
                <ComboBoxItem Content="Graphic Designer"/>
            </ComboBox>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Email Address" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtEmail" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="Phone Number" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtPhone" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
            </Grid>

            <TextBlock Text="LinkedIn Profile" Style="{StaticResource FieldLabel}"/>
            <TextBox x:Name="txtLinkedIn" Style="{StaticResource ModernTextBox}" Tag="https://linkedin.com/in/username"/>

            <TextBlock Text="GitHub / Portfolio Link" Style="{StaticResource FieldLabel}"/>
            <TextBox x:Name="txtGitHub" Style="{StaticResource ModernTextBox}"/>

            <TextBlock Text="Address" Style="{StaticResource FieldLabel}"/>
            <TextBox x:Name="txtAddress" Style="{StaticResource ModernTextBox}" Height="80" AcceptsReturn="True" VerticalContentAlignment="Top"/>
        </StackPanel>

        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
            <TextBlock Text="Profile Photo" Style="{StaticResource FieldLabel}" HorizontalAlignment="Center"/>

            <Border Width="180" Height="180" CornerRadius="90" BorderBrush="#EEEEEE" BorderThickness="2" Margin="0,10,0,15" Background="#F9F9F9">
                <Grid>
                    <TextBlock x:Name="txtPhotoPlaceholder" Text="👤" FontSize="80" Foreground="#CCCCCC" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Image x:Name="imgProfile" Stretch="UniformToFill">
                        <Image.Clip>
                            <EllipseGeometry RadiusX="90" RadiusY="90" Center="90,90"/>
                        </Image.Clip>
                    </Image>
                </Grid>
            </Border>

            <Button x:Name="btnUploadPhoto" Content="Upload Photo" Padding="15,8" Background="Transparent" BorderBrush="#007ACC" Foreground="#007ACC" Cursor="Hand" Click="btnUploadPhoto_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button x:Name="btnRemovePhoto" Content="Remove Photo" Margin="0,8,0,0" Padding="15,5" 
            Background="Transparent" BorderBrush="#FF4444" Foreground="#FF4444" 
            FontSize="12" Cursor="Hand" Visibility="Collapsed" Click="btnRemovePhoto_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>

            <TextBlock Text="Recommended: Square Image" FontSize="11" Foreground="#AAAAAA" Margin="0,10,0,0" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>