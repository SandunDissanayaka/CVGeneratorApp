<UserControl x:Class="CVGeneratorApp.LanguagesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White" FontFamily="Segoe UI">

    <UserControl.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        <Style x:Key="ModernInput" TargetType="ComboBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Language" Style="{StaticResource FieldLabel}"/>
                    <ComboBox x:Name="cmbLanguage" IsEditable="True" Style="{StaticResource ModernInput}">
                        <ComboBoxItem Content="Sinhala"/>
                        <ComboBoxItem Content="English"/>
                        <ComboBoxItem Content="Tamil"/>
                        <ComboBoxItem Content="Hindi"/>
                        <ComboBoxItem Content="French"/>
                        <ComboBoxItem Content="German"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="Proficiency Level" Style="{StaticResource FieldLabel}"/>
                    <ComboBox x:Name="cmbProficiency" Style="{StaticResource ModernInput}">
                        <ComboBoxItem Content="Native / Bilingual" IsSelected="True"/>
                        <ComboBoxItem Content="Full Professional Proficiency"/>
                        <ComboBoxItem Content="Working Proficiency"/>
                        <ComboBoxItem Content="Intermediate"/>
                        <ComboBoxItem Content="Beginner"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </StackPanel>

        <Button x:Name="btnAddLanguage" Grid.Row="1" Content="+ Add Language" 
                HorizontalAlignment="Left" Padding="20,10" Margin="0,0,0,20"
                Background="Transparent" BorderBrush="#007ACC" Foreground="#007ACC" 
                FontWeight="Bold" Cursor="Hand" Click="btnAddLanguage_Click">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Button.Resources>
        </Button>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="lstLanguages">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F9F9F9" CornerRadius="8" Padding="15" Margin="0,0,0,10" BorderBrush="#EEEEEE" BorderThickness="1">
                            <Grid>
                                <StackPanel>
                                    <TextBlock Text="{Binding LanguageName}" FontWeight="Bold" FontSize="15" Foreground="#333333"/>
                                    <TextBlock Text="{Binding Proficiency}" FontSize="13" Foreground="#007ACC" Margin="0,2,0,0"/>
                                </StackPanel>
                                <Button Content="Remove" HorizontalAlignment="Right" VerticalAlignment="Center" 
                                        Background="Transparent" Foreground="#FF4444" BorderThickness="0" Cursor="Hand"
                                        Click="RemoveLanguage_Click"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>